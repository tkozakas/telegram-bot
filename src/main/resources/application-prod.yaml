spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/telegram_bot
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    root: ERROR
